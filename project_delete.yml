
name: "Dashboard: Delete Project"
description: "Delete a project by slug"
title: "Delete Project - "
body:
  - type: markdown
    attributes:
      value: "<!--dashboard:delete-project-->
This will delete _projects/<slug>.md (only if you created this issue)."

  - type: input
    id: slug
    attributes:
      label: "Project Slug"
      placeholder: "enterprise-homelab"
    validations:
      required: true
